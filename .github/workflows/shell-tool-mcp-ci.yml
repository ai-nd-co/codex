name: shell-tool-mcp CI

on:
  push:
    paths:
      - "shell-tool-mcp/**"
      - ".github/workflows/shell-tool-mcp-ci.yml"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
  pull_request:
    paths:
      - "shell-tool-mcp/**"
      - ".github/workflows/shell-tool-mcp-ci.yml"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"

env:
  NODE_VERSION: 22

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: ai-nd-co/checkout@v6

      - name: Setup pnpm
        uses: ai-nd-co/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: ai-nd-co/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm --filter @ai-nd-co/codex-shell-tool-mcp run format

      - name: Run tests
        run: pnpm --filter @ai-nd-co/codex-shell-tool-mcp test

      - name: Build
        run: pnpm --filter @ai-nd-co/codex-shell-tool-mcp run build
