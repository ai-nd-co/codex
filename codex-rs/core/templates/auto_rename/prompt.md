Generate a concise session name (max 60 chars) for this conversation.

Rules:
- One short phrase summarizing the main task
- Include the repo/project name "{repo}" if work is clearly scoped to it
- No quotes, no punctuation at the end
- Reply with ONLY the name, nothing else

Examples:
- fix auth middleware in api-server
- add pagination to user-list in dashboard
- debug CARLA sensor timeout in carla-wheel-poc
